{
  "name": "server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "build": "bun build index.ts --bundle --minify --target=bun --external bullmq --external ioredis --external @aws-sdk/client-ses --outfile dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.0",
    "dotenv": "^17.4.2",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.1116.0",
    "bullmq": "^6.2.0",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.12.23",
    "ioredis": "^6.0.0",
    "pg": "^8.13.0",
    "pino": "^10.3.1"
  }
}
